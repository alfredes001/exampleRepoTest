/// <reference types="cypress" />

describe('template spec', () => {
  it('passes', () => {
    // test to test to test
    cy.visit('https://example.cypress.io')
  })
  it('passes', () => {
    // test to test to test
    cy.visit('https://example.cypress.io')
  })
})